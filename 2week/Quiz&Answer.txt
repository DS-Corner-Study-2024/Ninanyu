1. 프로그램에서 변수나 함수를 호출하거나 접근하는 코드가 함수 선언보다 위에 있음에도 불구하고, 마치 선언 코드가 위에 있는 것처럼 동작하는 기능을 (호이스팅)이라고 한다.

2. 자바스크립트의 변수와 함수는 생성과 동시에 접근하거나 호출할 때 일정한 제약을 갖는데 이를 (스코프)라고 한다.

3. 함수를 호출하면서 넘겨주는 '값'을 (인수), 함수에서 넘겨받은 인수를 저장하는 '변수'를 (매개변수)라고 한다.

4. 빈 배열을 생성할 때 객체 리터럴은 (중괄호)를 배열 리터럴은 (대괄호)를 이용한다.

5. (원시 자료형)은 하나의 값을 저장하지만 함수와 배열 같은 (객체 자료형)은 여러 개의 값을 저장한다.

6.인덱스는 배열 요소의 위치를 (0)부터 시작하는 숫자로 순서대로 표현한 것이다.

7. 객체 메서드인 (Object.keys)는 객체 프로퍼티의 key를 배열로 반환한다.

코드 퀴즈
문제1 
function greet(){
    console.log("안녕 내 닉네임은 니나뉴야");
}
위 코드를 닉네임을 매개변수로 전달받아 다양한 닉네임을 프린트 할 수 있게 함수를 수정하고 매개변수로 전달받은 닉네임을 반환하게 함수를 수정하시오 

예) “안녕 내 닉네임은 000야"
문제 2. 돈을 매개변수로 받으면 몇개의 지폐와 동전이 필요한지 최소 개수를 계산해주는 함수를 만드시오. (math.floor [소수값 버림] 사용)
ex)값이 12300인 경우 50000 X0, 10000 X1, 5000 X0, 1000 X2, 500 X0, 100 X3